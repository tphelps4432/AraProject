<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:orientation="vertical"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="#FFFFFF"
    tools:context="com.example.tomphelps.araproject.TestingActivity">

    <ScrollView
        android:id="@+id/testScrollView"
        android:layout_weight=".1"
        android:layout_width="fill_parent"
        android:layout_alignParentTop="true"
        android:layout_height="fill_parent">

        <TextView
            android:text="@string/hello_world"
            android:scrollbars="vertical"
            android:id="@+id/testingTextView"
            android:layout_width="fill_parent"
            android:gravity="center"
            android:layout_height="fill_parent"
            android:textColor="#000000" />
    </ScrollView>

    <Button
        android:id="@+id/singleReadButton"
        android:text="Start Chip"
        android:onClick="startChip"
        android:layout_above="@+id/continiousRead"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/continiousRead"
        android:text="Stop Chip"
        android:layout_above="@+id/testButton"
        android:onClick="stopChip"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/testButton"
        android:text="Test Button"
        android:onClick="singleSend"
        android:layout_alignParentBottom="true"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" />

</LinearLayout>
